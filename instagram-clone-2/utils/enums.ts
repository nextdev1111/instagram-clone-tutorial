export enum userLoading {
  IDLE = "idle",
  LOGIN = "login",
  REGISTER = "register",
  FORGOTPASSWORD = "forgotpassword",
  PASSWORDRESET = "passwordreset",
  EMAILVERIFICATIONREQUEST = "emailverificationrequest",
}

export enum postLoading {
  IDLE = "idle",
  CREATE = "create",
}
